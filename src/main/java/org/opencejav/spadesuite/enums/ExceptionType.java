package org.opencejav.spadesuite.enums;

public enum ExceptionType {
    // File Specific Exceptions
    INV_FILE_PATH,
    INV_FILE_TYPE,
    INV_FILE_FORMAT,
    INV_FILE_CONTENT,
    INV_FILE_NAME,

    INV_SESSION_TIMEOUT,
    INV_CONNECTION_TIMEOUT,
    INV_POOL_SIZE,
    INV_DETECTION_THRESHOLD,
}
